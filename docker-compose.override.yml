services:
  frontend:
    build: "./frontend"
    ports:
      - "3000:3000"

  backend:
    build: "./backend"
    command: "--reload --log-level trace"
    ports:
      - "8000:8000"
    volumes:
      - "./backend:/usr/src/kayman"

  db:
    ports:
      - "5432:5432"
