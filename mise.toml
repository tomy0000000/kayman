[tools]
hadolint = "2.14.0"
node = "22.16.0"
pnpm = "10.12.1"
python = "3.12"
uv = "0.10.10"

[task_config]
includes = ["scripts", "backend/scripts", "frontend/scripts"]

[tasks.start]
description = "Start all dev servers"
depends = ["start:backend", "start:frontend"]
